server:
  port: 8080

spring:
  security:
    user:
      name: admin
      password: admin
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pfsr98/config-repo.git
          default-label: main